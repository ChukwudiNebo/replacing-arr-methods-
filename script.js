// const array=['chukwudi',3,'rice'];

// // console.log(map(array,(array)=>{array}));
// const mapped = map(array,(array)=>{array})
// (mapped)();


// (() => {
  
// })();
let arr =['sdfds']
for(let i=0;i<arr.length;i++){
    let array=[]
    console.log(arr[i]+'klsdf')
    // return array


// i pushed 2 to the new array 
    // array.push(i+2)
    // console.log(array)
}

